{% set data = load_file_data("../pyproject.toml", from_recipe_dir=True) %}

package:
  name: "{{ data.get('project', {}).get('name')|lower }}"
  version: "{{ data.get('project', {}).get('version') }}"

source:
  path: ../

build:
  number: 0

requirements:
  host:
    - python=3.10
    - pip
    - setuptools>=61.0
    - wheel

  run:
    - argcomplete=3.6
    - biopython
    - blast=2
    - bwa>=0.7
    - coreutils
    - edlib=1.3
    - entrez-direct
    - goatools
    - mamba=1
    - matplotlib=3.10
    - mosdepth
    - numpy=1.26.4
    - pandas
    - parallel
    - pigz
    - pip
    - pulp=2.7.0
    - pybedtools
    - pyfastx
    - pytest
    - pytest-cov
    - python=3.10
    - rich
    - rich-argparse=1.7.1
    - samtools
    - scikit-bio
    - snakemake=7.26.0
    - taxonkit>=0.20.0
    - tqdm>=4.66.5
    - unzip
    - zip


test:
  commands:
    bit-version

about:
  home: https://github.com/AstrobioMike/bit
  license: GPL3
  summary: A set of bioinformatics tools and workflows I use regularly enough to want to have accessible from anywhere.

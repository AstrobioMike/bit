[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bit"
version = "1.17.0"
description = "A set of bioinformatics scripts and workflows"
readme = "README.md"
license = { text = "GPLv3" }
authors = [{ name = "Mike Lee", email = "Michael.Lee0517@gmail.com" }]
urls = { Repository = "https://github.com/AstrobioMike/bit" }
# dependencies handled by conda installation

[tool.setuptools]
packages = { find = { where = ["."], include = ["bit*"] } }
include-package-data = true

[tool.setuptools.package-data]
bit = ["tests/data/*", "smk/*.smk", "smk/envs/*"]

[project.scripts]
bit-version = "bit.modules.general:report_version"


## ncbi/GTDB-related ##
bit-dl-ncbi-assemblies = "bit.cli.dl_ncbi_assemblies:main"
# bit-get-accessions-from-GTDB -> bit-accessions-from-gtdb?
# bit-get-lineage-from-taxids -> bit-lineage-from-taxids?


## coverage / mapping ##
bit-cov-analyzer = "bit.cli.cov_analyzer:main"
bit-cov-stats = "bit.cli.cov_stats:main"
bit-mapped-reads-pid = "bit.cli.mapped_reads_pid:main"


## sequence utilities ##
bit-gen-reads = "bit.cli.gen_reads:main"
bit-mutate-seqs = "bit.cli.mutate_seqs:main"
bit-add-insertion = "bit.cli.add_insertion:main"
bit-summarize-assembly = "bit.cli.summarize_assembly:main"


bit-fasta = "bit.cli.fasta:main"
    # subcommands:
        # calc-gc
        # calc-var-in-msa
        # count
        # extract-by-coords
        # extract-by-headers
        # extract-by-primers
        # filter-by-length
        # to-bed
        # to-genbank

    # still need to combine the below with subcommands into bit-fasta
        # bit-rename-fasta-headers -> bit-fasta modify-headers
        # bit-remove-wraps -> bit-fasta remove-wraps


## table utilities ##
bit-colnames = "bit.cli.colnames:main"
bit-summarize-column = "bit.cli.summarize_column:main"
bit-normalize-table = "bit.cli.normalize_table:main"
# bit-filter-table


## miscellaneous ##
bit-assemble = "bit.cli.assemble:main"
bit-ez-screen = "bit.cli.ez_screen:main"
bit-get-test-data = "bit.cli.get_test_data:main" # maybe add this as a subcommand to bit-data
bit-get-workflow = "bit.cli.get_workflow:main"
bit-lineage-to-tsv = "bit.cli.lineage_to_tsv:main"


## kraken2/bracken helpers ##
bit-kraken2-tax-plots = "bit.cli.kraken2_tax_plots:main"
bit-kraken2-tax-summary = "bit.cli.kraken2_tax_summary:main"
    # put these all under bit-kraken2 with subcommands:
        # tax-plots
        # tax-summary


## Gene Ontology (GO) helpers ##
# bit-summarize-go-annotations
# bit-slim-down-go-terms
# bit-get-go-term-info
    # put these all under bit-go with subcommands:
        # summarize-annotations
        # slim-down-terms
        # get-term-info


## iToL helpers ##
bit-itol = "bit.cli.itol:main"

## genbank manipulations ##
bit-genbank = "bit.cli.genbank:main"

## Database related ##
bit-data-locations = "bit.cli.data_locations:main"
get-ncbi-assembly-data = "bit.modules.ncbi.get_ncbi_assembly_data:main" # move to bit-data program eventually
get-ncbi-tax-data = "bit.modules.ncbi.get_ncbi_tax_data:main"           # move to bit-data program eventually
bit-update-ncbi-taxonomy = "bit.modules.ncbi.get_ncbi_tax_data:main"    # move to bit-data program eventually
    # get-ncbi-tax-data and bit-update-ncbi-taxonomy can probably be combined into below `bit-data get ncbi-tax-data`

# get-go-dbs
# get-gtdb-metadata

    # do a subcommand situation for these
    # bit-data
        # locations
            # check/set
        # get (with optional --update flags for all)
            # ncbi-assembly-data
            # ncbi-tax-data
            # go-dbs
            # gtdb-metadata

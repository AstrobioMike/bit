[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "bit"
version = "1.16.0"
description = "A set of bioinformatics scripts and workflows"
readme = "README.md"
license = { text = "GPLv3" }
authors = [{ name = "Mike Lee", email = "Michael.Lee0517@gmail.com" }]
urls = { Repository = "https://github.com/AstrobioMike/bit" }
# dependencies handled by conda installation

[tool.setuptools]
packages = { find = { where = ["."], include = ["bit*"] } }
include-package-data = true

[tool.setuptools.package-data]
bit = ["tests/data/*", "smk/*.smk", "smk/envs/*"]

[project.scripts]
bit-version = "bit.modules.general:report_version"


## ncbi/GTDB-related ##
bit-dl-ncbi-assemblies = "bit.cli.dl_ncbi_assemblies:main"
# bit-get-accessions-from-GTDB
# bit-get-lineage-from-taxids


## coverage / mapping ##
bit-cov-analyzer = "bit.cli.cov_analyzer:main"
bit-cov-stats = "bit.cli.cov_stats:main"
bit-mapped-reads-pid = "bit.cli.mapped_reads_pid:main"


## sequence utilities ##
bit-extract-seqs = "bit.cli.extract_seqs:main"
bit-gen-reads = "bit.cli.gen_reads:main"
bit-mutate-seqs = "bit.cli.mutate_seqs:main"
bit-add-insertion = "bit.cli.add_insertion:main"

bit-calc-gc-per-seq = "bit.cli.calc_gc_per_seq:main"
bit-calc-gc-sliding-window = "bit.cli.calc_gc_sliding_window:main"
bit-calc-variation-in-msa = "bit.cli.calc_variation_in_msa:main"
bit-count-bases = "bit.cli.count_bases:main"
bit-dedupe-fasta-headers = "bit.cli.dedupe_fasta_headers:main"
bit-fasta-to-bed = "bit.cli.fasta_to_bed:main"
bit-fasta-to-genbank = "bit.cli.fasta_to_genbank:main"
bit-filter-fasta-by-length = "bit.cli.filter_fasta_by_length:main"
# bit-remove-wraps
# bit-rename-fasta-headers -> bit-simplify-fasta-headers
bit-summarize-assembly = "bit.cli.summarize_assembly:main"

    # maybe combine the below with subcommands into bit-fasta
        # bit-calc-gc-per-seq -> bit-fasta calc-gc (combine per-seq and overall like i did with bit-count-bases)
        # bit-calc-gc-sliding-window -> bit-fasta calc-gc-sliding-window (maybe combine this with above... would make the help menu a lot messier though if i did)
        # bit-calc-variation-in-msa -> bit-fasta calc-variation-in-msa
        # bit-count-bases -> bit-fasta count-bases
        # bit-dedupe-fasta-headers -> bit-fasta dedupe-headers
        # bit-fasta-to-bed -> bit-fasta to-bed
        # bit-fasta-to-genbank -> bit-fasta to-genbank
        # bit-filter-fasta-by-length -> bit-fasta filter-by-length (or put in bit-extract-seqs as mentioned above)
        # bit-rename-fasta-headers -> bit-fasta simplify-headers
        # bit-remove-wraps -> bit-fasta remove-wraps
        # bit-summarize-assembly -> bit-fasta summarize



## fastq stuff
bit-check-fastq-for-dup-headers = "bit.cli.check_fastq_for_dup_headers:main" # maybe move to gist only (need to make it standalone first)
# bit-parse-fastq-by-headers # can maybe put this in extract-seqs if i don't tie that to bit-fasta (or maybe just make this a gist too, until i add more fastq stuff)


## table utilities ##
bit-colnames = "bit.cli.colnames:main"
bit-summarize-column = "bit.cli.summarize_column:main"
bit-normalize-table = "bit.cli.normalize_table:main"
# bit-filter-table


## miscellaneous ##
bit-assemble = "bit.cli.assemble:main"
bit-ez-screen = "bit.cli.ez_screen:main"
bit-get-test-data = "bit.cli.get_test_data:main"
bit-get-workflow = "bit.cli.get_workflow:main"
bit-lineage-to-tsv = "bit.cli.lineage_to_tsv:main"


## kraken2/bracken helpers ##
bit-kraken2-tax-plots = "bit.cli.kraken2_tax_plots:main"
bit-kraken2-tax-summary = "bit.cli.kraken2_tax_summary:main"
    # put these all under bit-kraken2 with subcommands:
        # tax-plots
        # tax-summary


## Gene Ontology (GO) helpers ##
# bit-summarize-go-annotations
# bit-slim-down-go-terms
# bit-get-go-term-info
    # maybe put these all under bit-go with subcommands:
        # summarize-annotations
        # slim-down-terms
        # get-term-info


## iToL helpers ##
bit-itol = "bit.cli.itol:main"

## genbank manipulations ##
bit-genbank = "bit.cli.genbank:main"

## Database related ##
bit-data-locations = "bit.cli.data_locations:main"
get-ncbi-assembly-data = "bit.modules.ncbi.get_ncbi_assembly_data:main" # move to bit-data program eventually
get-ncbi-tax-data = "bit.modules.ncbi.get_ncbi_tax_data:main"           # move to bit-data program eventually
bit-update-ncbi-taxonomy = "bit.modules.ncbi.get_ncbi_tax_data:main"    # move to bit-data program eventually
    # get-ncbi-tax-data and bit-update-ncbi-taxonomy can probably be combined into below `bit-data get ncbi-tax-data`

# get-go-dbs
# get-gtdb-metadata

    # do a subcommand situation for these
    # bit-data
        # locations
            # check/set
        # get (with optional --update flags for all)
            # ncbi-assembly-data
            # ncbi-tax-data
            # go-dbs
            # gtdb-metadata

# add -w (as -t) to bit dl-ncbi-assemblies
    # started this, but i have work to do here testing/adjusting maybe, poke at it a bit

# test taxon selection in all functions that use it
    # dl-ncbi-assemblies seems to be working
    # get-accs-from-*
    # others?
# keep mirroring help menus with gtotree where possible


# fix -a, --assembly-level help menu/parser to match gtt dl-ncbi-assemblies in both that program and get-accs-from-ncbi

